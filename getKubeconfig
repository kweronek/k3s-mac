# set kubeconfig for cluster master_node_name $1
echo "set kubeconfig for cluster master_node_name $1"


#!/bin/bash
#multipass exec uk8s-master-0 -- /bin/bash -c "sudo microk8s.config view" > kubeconfig
multipass transfer $1:/etc/rancher/k3s/k3s.yaml .kubeconfig
echo "please execute:"
echo "export KUBECONFIG=./kubeconfig"

